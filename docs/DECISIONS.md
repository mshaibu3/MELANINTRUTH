# Decisions

* Use FastAPI, SQLModel, Alembic, PostgreSQL-ready persistence.
* Use inclusive baseline segmentation to avoid excluding dark skin, vitiligo, hyperpigmentation, or hypopigmentation.
* Use identity-preserving baseline rendering until validated trained D65 model is registered.
